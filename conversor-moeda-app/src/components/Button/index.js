import { Text, Touchable, TouchableOpacity } from "react-native";

export default function Button(){
    return(
        <TouchableOpacity>
            <Text>

            </Text>
        </TouchableOpacity>
    )
}